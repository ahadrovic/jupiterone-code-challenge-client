import React from 'react';

import styles from './PwnedLogo.module.css';

const PwnedLogo = () => {
  return <span className={styles.logoSpan}>';--have i been pwned?</span>;
};

export default PwnedLogo;
